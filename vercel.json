{
  "version": 2,
  "projects": [
    {
      "name": "server",
      "path": "server",
      "buildCommand": "npm run build",
      "outputDirectory": "dist"
    },
    {
      "name": "client",
      "path": "client",
      "buildCommand": "npm run build",
      "outputDirectory": "dist"
    }
  ]
}
